Streaming Ollama tools patch

Requires:
- existing ToolCallingChat implementation

Modifies:
- voice_chat/tooling.py only

Changes:
- /api/chat tool requests use stream=true
- parse NDJSON incrementally with requests.iter_lines()
- yield ordinary content chunks immediately
- accumulate thinking/content/tool_calls for tool-loop history
- execute tools after each streamed round completes
- stream final post-tool answer
- preserve base.chat_stream fallback for unsupported tools

Preserves:
- tool registry
- web_search/web_fetch
- config.toml
- audio/TTS/barge-in
- voice presets
- memory settings
